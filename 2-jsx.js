// We call HTML attributes props in JSX.
// JSX props are camelCased except for custom data and aria props.

// JSX uses className rather than class.
// JSX uses htmlFor rather than for.
// The style prop accepts an object rather than a string.

// In JSX we use {} to escape JSX and execute pure JavaScript.
// If the prop value is text: use "", otherwise use {}. 

// Every JSX tag must be closed( either self-closing or using a closing tag).

// In JSX, we must return only a single top level element.
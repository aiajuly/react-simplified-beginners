// React breaks the UI into smaller, reusable chunks of code that we call components.

// React uses JSX which is a special syntax that combines HTML and JavaScript; React will then transpile it to plain JavaScript.